/**
 * Description for the footer of a parquet file.
 *
 * The footer is at the end of the file and contains the FileMetaData, 
 * the FileMetaData is stored in a compact format to save space.
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed) 
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3. FileMetaData (uncompressed and unencrypted)
 *
 * The FileMetaData is stored in the footer using a compact format to save space.
 * The structure of the footer is as follows:
 * 1. FileMetaData (compressed and encrypted if needed)
 * 2. FileMetaData (uncompressed and encrypted if needed)
 * 3.